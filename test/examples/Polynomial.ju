mod Polynomial where

open Prelude
open Circuit

sig (+) : field -> field -> field
let (+) = %Circuit.add

-- sig fn : field -> field -> field
-- let fn x y = (+) x x -- x^3 + 2x^2 + 1 == y 

sig main : field -> field -> field
let main = \x y -> (+) x y