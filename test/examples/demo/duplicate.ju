mod Duplicate where

open Prelude
open Michelson

sig cons-pair : list operation -> pair nat nat -> pair (list operation) (pair nat nat)
let cons-pair = %Michelson.pair

sig cons-pair2 : nat -> nat -> pair nat nat
let cons-pair2 = %Michelson.pair

sig nil : list operation
let nil = %Michelson.nil

sig car : pair nat (pair nat nat) -> nat
let car = %Michelson.car

sig main : pair nat (pair nat nat) -> pair (list operation) (pair nat nat)
let main = \ps -> cons-pair nil (cons-pair2 (car ps) (car ps))
