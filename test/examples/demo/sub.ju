open Prelude
open Prelude.Michelson

sig main : pair nat nat -> pair nat (list operation)
let main = \ps ->
  cons-pair
    (list operation)
    nat
    (nil operation)
    (sub nat (car nat nat ps) (cdr nat nat ps))
