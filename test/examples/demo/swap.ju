open Prelude
open Prelude.Michelson

sig storage : ty
let storage = pair int int

sig param : ty
let param = unit-t

sig main : param -> storage -> pair storage (list operation)
let main = \_ storage -> cons-pair (cons-pair (snd storage) (fst storage)) nil
