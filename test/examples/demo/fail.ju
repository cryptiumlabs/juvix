mod Fail where

open Prelude
open Michelson

sig main : pair int int -> pair (list operation) int
let main = \pair ->
  cons-pair
    (list operation)
    int
    nil
    (if2 int
         (compare 0 (car int int pair))
         (fail-with 0)
         (cdr int int pair))
